# Password-Validator
A Java Swing-based password validation application that checks user input against security rules like length, character types (uppercase, lowercase, digit, special), and balanced brackets using a stack. It uses object-oriented design and custom exception handling to display clear validation messages without crashing the program.
